from db.session import get_db_session

# This file can just re-export the get_db_session from db.session
# or we can move the get_db_session function here.
# For now we just import it to keep the structure.
